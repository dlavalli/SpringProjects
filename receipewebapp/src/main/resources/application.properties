# Set the active profile to use application-dev.yml and other configured files
#spring.profiles.active=dev

#spring.h2.console.enabled=true

#spring.datasource.initialization-mode=always
spring.jpa.defer-datasource-initialization: true

# Adding debug level to the application
logging.level.com.lavalliere.daniel.spring.recipewebapp=debug
