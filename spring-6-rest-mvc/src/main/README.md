This project contains multiple test folders.
- The Default test forlder that runs normla unit tests
- The test-restassured folder to execute REST assured related tests, trigger with:   mvn test -Ptest-restassured
- The test-karate folfer to execute Karate based test, trigger with:  mvn test -Ptest-karate
